import AnimateText from "./AnimateText";

const SectionSecond = ({scrollYProgress}) => {

    return (
      <div className="sticky top-0 w-full h-screen bg-[#01268B] text-[3.5vw] flex flex-col items-center justify-center text-white">
       
   
        <AnimateText />
        

      </div>
    )
}

export default SectionSecond
